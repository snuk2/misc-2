#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include "word_library.h"

using namespace std;


int main() {
    // Calls test functions to check if loadDictionary and containsWord are working properly
    testLoadDictionary();
    testContainsWord();
    
    // Create an empty vector of strings that will hold the dictionary words
    

    // Create an input filestream to read from "words.txt" (these are your dictionary words)
    

    // Use loadDictionary function to fill your empty vector with words from the input file
    

    // Create an input filstream to read from "essay.txt" in order to spellcheck it


    // Create an output filestream called "essay_checked.txt" for the checked essay
    

    // Read through the essay and check each word, if it is not a real word, mark it with asterisks
    
    
    
    
    
    // Uncomment the two lines below 
    // cout << "Spellchecking finished!" << endl;
    // cout << "Check essay_checked.txt for results." << endl;

}